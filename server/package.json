{
  "name": "@orbitalpha/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "npm run build -w @orbitalpha/shared",
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/index.ts",
    "start": "npm run start:build",
    "start:build": "npm run build && npm run start:runtime",
    "start:runtime": "node dist/index.js",
    "start:surge-shadow": "node dist/worker-surge-scanner-shadow.js",
    "//": "start:surge-v2-worker: New Surge V2 shadow engine. Note: Concurrent use with engine1 scanner doubles Upbit load.",
    "start:surge-v2-worker": "node dist/surge-v2-worker.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@orbitalpha/shared": "*",
    "@fastify/cors": "^10.0.2",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
